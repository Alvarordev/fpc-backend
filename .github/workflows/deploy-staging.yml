name: Deploy staging

on:
  push:
    branches:
      - develop

permissions:
  contents: read
  packages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/alvarordev/fpc-backend:dev-${{ github.sha }}
            ghcr.io/alvarordev/fpc-backend:dev-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trigger Dokploy
        run: curl -X POST "${{ secrets.DOKPLOY_WEBHOOK_STAGING }}"
